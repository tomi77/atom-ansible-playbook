'use babel';

import AnsiblePlaybookView from '../lib/ansible-playbook-view';

describe('AnsiblePlaybookView', () => {
  it('has one valid test', () => {
    // expect('life').toBe('easy');
  });
});
